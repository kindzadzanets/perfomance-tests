# Устанавливается базовый образ Python версии 3.11
FROM python:3.11
# Устанавливается рабочая директория внутри контейнера
WORKDIR /app
# Копируются все файлы из текущей директории на хосте в рабочую директорию контейнера
COPY . .
# Выполняется установка библиотеки termcolor с помощью pip
RUN pip install termcolor
# Определяется команда по умолчанию для запуска контейнера: запуск файла docker_example.py через Python
CMD ["python", "docker_example.py"]
