```json
/*
 JSON (JavaScript Object Notation) — текстовый формат обмена данными.
 Основан на структуре "ключ-значение" и поддерживает объекты, массивы, строки, числа, булевы значения и null.
 В отличие от XML, JSON компактнее и чаще используется для передачи данных в веб-приложениях.
*/

/* Корневой объект заключён в фигурные скобки { }. Внутри описаны поля с данными о пользователе. */
{
    /* Строковое поле: имя пользователя */
    "name": "Ivan",

    /* Числовое поле: возраст */
    "age": 10,

    /* Булево поле: является ли студентом */
    "is_student": false,

    /* Массив (список) курсов.
       Может содержать строки и вложенные объекты. */
    "courses": [
        "python",
        "QA automation",
        "API Testing",
        {
            /* Вложенный объект внутри массива */
            "name": "any name"
        }
    ],

    /* Вложенный объект address хранит адрес */
    "address": {
        /* Название города */
        "city": "Moscow",

        /* Почтовый индекс */
        "zip": "000100"
    }
}
```
